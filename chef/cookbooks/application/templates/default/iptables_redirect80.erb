*nat
# Redirect anything on eth0 coming to port 80 to the frontend server port
-A PREROUTING -i eth0 -p tcp --dport 80 -j REDIRECT --to-port <%= node.default['application']['frontend']['port'] %>
-I OUTPUT -p tcp -o lo --dport 80 -j REDIRECT --to-ports <%= node.default['application']['frontend']['port'] %>
